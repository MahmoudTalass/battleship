/* eslint-disable no-undef */
describe("Gameboard tests", () => {
    let gameBoard;
    beforeEach(()=> {
        gameBoard = new Gameboard();
    })
    test("Place ship on board", ()=> {
        expect
    })
})